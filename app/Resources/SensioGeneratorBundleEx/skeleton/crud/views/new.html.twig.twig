{% block extends %}
{{ "{% extends 'HVGSystemBundle:System:base.html.twig' %}" }}
{% endblock extends %}

{% block body %}
{{ "{% block title %}" }}{{ "{{ '" ~ entity_singularized|lower ~ ".new.title'|trans({}, '" ~ bundle ~ "') }}" }}{{ "{% endblock %}" }}

{{ "{% block body %}" }}
    <div id="page-wrapper">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">{{ "{% include 'HVGSystemBundle:System:icon.html.twig' %}" }}{{ "{{ '" ~ entity_singularized|lower ~ ".new.title'|trans({}, '" ~ bundle ~ "') }}" }}</h1>
                {{ "{% include 'HVGSystemBundle:System:flashbag.html.twig' %}" }}
            </div>
            <!-- /.col-lg-12 -->
        </div>
        <!-- /.row -->
        <div class="row">
            <div class="col-lg-12">
                <div class="panel panel-default">

                    {%- set hide_edit, hide_delete = true, true -%}
                    {{- include('crud/views/others/record_actions.html.twig.twig') }}
                    <div class="panel-body">
                        {{ "{{ form_start(newForm, { 'style': 'horizontal' }) }}" }}
                            {{ '{{ form_widget(newForm) }}' }}
                                <div class="form-group"><div class="col-lg-offset-4 col-lg-8">
                                    <button type="submit" class="btn btn-primary">{{ "{{ icon('save') }}&nbsp;{{ '" ~ entity_singularized|lower ~ ".new.submit'|trans({}, '" ~ bundle ~ "') }}" }}</button>
                                </div></div>
                        {{ '{{ form_end(newForm) }}' }}

                    </div>
                </div>
            </div>
            <!-- /.col-lg-12 -->
        </div>
        <!-- /.row -->
    </div>
    <!-- /#page-wrapper -->

{{ "{% endblock %}" }}
{% endblock body %}


