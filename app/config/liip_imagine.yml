liip_imagine:
    resolvers:
        default:
            web_path: ~
    filter_sets:
        cache: ~
        logo_login:
            quality: 75
            filters:
                upscale: { min: [800, 900] }
                thumbnail: { size: [800, 900], mode: outbound }
