{% extends 'HVGAgentBundle:Carpark:layout.html.twig' %}

{% set translation_domain = 'HVGAgentBundle' %}{% trans_default_domain translation_domain %}

{% block title %}{{ 'carpark.delete.title'|trans }}{% endblock %}

{% block carpark %}
    <div class="row" id="page-delete">
        <div class="col-md-6">
            <div class="panel panel-danger">
                <div class="panel-heading">
                    <h3 class="panel-title">{{ 'carpark.delete.title'|trans }}</h3>
                </div>
                <div class="panel-body">
                    <dl class="dl-horizontal">
                        <dt>{{ 'carpark.form.name'|trans }}</dt><dd>{% if carpark.name %}{{ carpark.name }}{% endif %}</dd>
                        <dt>{{ 'carpark.form.unit'|trans }}</dt><dd>{% if carpark.unit %}{{ carpark.unit }}{% endif %}</dd>
                    </dl>
                </div>
            </div>
        </div>
        {{ form_start(deleteForm) }}
        {{ form_widget(deleteForm) }}
        <div class="col-md-6">
            <div class="btn-toolbar">
                <div class="btn-group"><button type="submit" class="btn btn-danger">{{ icon('times fa-fw') }}&nbsp;{{ 'carpark.delete.submit'|trans }}</button></div>
                <div class="btn-group"><a type="button" class="btn btn-default" href="{{ path('agent_carpark_show', {'carpark' : carpark.id}) }}">{{ icon('times fa-fw') }}&nbsp;{{ 'carpark.delete.cancel'|trans }}</a></div>
            </div>
        </div>
        {{ form_end(deleteForm) }}
    </div>
    <!-- /#page-list -->
{% endblock %}