{% block tools %}
    {% trans_default_domain 'HVGAgentBundle' %}
    <div class="row" id="tabs">
        <div class="col-md-12">
            {% if community %}
            <div class="btn-toolbar">
                <div class="btn-group">
                    <a href="{{ path('agent_carpark_new', {'community' : community.id}) }}" class="btn btn-success">{{ icon('plus fa-fw') }}&nbsp;{{ 'carpark.new.link'|trans }}</a>
                </div>
            </div>
            <p></p>
            {% endif %}
        </div>
    </div>
    <!-- /#unit-tab -->
{% endblock %}