{% extends 'HVGAgentBundle:GuestCarpark:layout.html.twig' %}

{% set translation_domain = 'HVGAgentBundle' %}{% trans_default_domain translation_domain %}

{% block title %}{{ 'guestcarpark.delete.title'|trans }}{% endblock %}

{% block guestcarpark %}
    <div class="row" id="page-delete">
        <div class="col-md-6">
            <div class="panel panel-danger">
                <div class="panel-heading">
                    <h3 class="panel-title">{{ 'guestcarpark.delete.title'|trans }}</h3>
                </div>
                <div class="panel-body">
                    <dl class="dl-horizontal">
                        <dt>{{ 'guestcarpark.form.name'|trans }}</dt><dd>{% if guestcarpark.name %}{{ guestcarpark.name }}{% endif %}</dd>
                        <dt>{{ 'guestcarpark.form.community'|trans }}</dt><dd>{% if guestcarpark.community %}{{ guestcarpark.community }}{% endif %}</dd>
                    </dl>
                </div>
            </div>
        </div>
        {{ form_start(deleteForm) }}
        {{ form_widget(deleteForm) }}
        <div class="col-md-6">
            <div class="btn-toolbar">
                <div class="btn-group"><button type="submit" class="btn btn-danger">{{ icon('times fa-fw') }}&nbsp;{{ 'guestcarpark.delete.submit'|trans }}</button></div>
                <div class="btn-group"><a type="button" class="btn btn-default" href="{{ path('agent_guestcarpark_show', {'guestcarpark' : guestcarpark.id}) }}">{{ icon('times fa-fw') }}&nbsp;{{ 'guestcarpark.delete.cancel'|trans }}</a></div>
            </div>
        </div>
        {{ form_end(deleteForm) }}
    </div>
    <!-- /#page-list -->
{% endblock %}