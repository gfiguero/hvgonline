{% block tools %}
    {% trans_default_domain 'HVGAgentBundle' %}
    <div class="row" id="tools">
        <div class="col-md-12">
            <div class="btn-toolbar">
                <div class="btn-group"><a href="{{ path('agent_person_index') }}" class="btn btn-default">{{ icon('list fa-fw') }}&nbsp;{{ 'person.index.link'|trans }}</a></div>
                <div class="btn-group"><a href="{{ path('agent_person_new') }}" class="btn btn-success">{{ icon('plus fa-fw') }}&nbsp;{{ 'person.new.link'|trans }}</a></div>
            </div>
            <p></p>
        </div>
    </div>
    <!-- /#tools -->
{% endblock %}