{% block tools %}
    {% trans_default_domain 'HVGAgentBundle' %}
    <div class="row" id="tabs">
        <div class="col-md-12">
            {% if unit %}
            <div class="btn-toolbar">
                <div class="btn-group">
                    <a href="{{ path('agent_pet_new', {'unit' : unit.id}) }}" class="btn btn-success">{{ icon('plus fa-fw') }}&nbsp;{{ 'pet.new.link'|trans }}</a>
                </div>
            </div>
            <p></p>
            {% endif %}
        </div>
    </div>
    <!-- /#unit-tab -->
{% endblock %}