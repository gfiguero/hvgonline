{% block tools %}
    {% trans_default_domain 'HVGConfigurationBundle' %}
    <div class="row" id="tabs">
        <div class="col-md-12">
            <div class="btn-toolbar">
                <div class="btn-group">
                    <a href="{{ path('configuration_service_new') }}" class="btn btn-success">{{ icon('plus fa-fw') }}&nbsp;{{ 'service.new.link'|trans }}</a>
                </div>
            </div>
            <p></p>
        </div>
    </div>
    <!-- /#unit-tab -->
{% endblock %}