{% block tools %}
    {% trans_default_domain 'HVGExchangeBundle' %}
    <div class="row" id="tabs">
        <div class="col-md-12">
            {% if unit %}
            <div class="btn-toolbar">
                <div class="btn-group">
                    <a href="{{ path('exchange_ticket_new', {'unit' : unit.id}) }}" class="btn btn-success">{{ icon('plus fa-fw') }}&nbsp;{{ 'ticket.new.link'|trans }}</a>
                </div>
            </div>
            <p></p>
            {% endif %}
        </div>
    </div>
    <!-- /#unit-tab -->
{% endblock %}