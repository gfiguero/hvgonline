{% extends 'HVGServiceControlBundle:Base:layout.html.twig' %}

{% trans_default_domain 'HVGServiceControlBundle' %}

{% block title %}{{ 'serviceagent.index.title'|trans }}{% endblock %}

{% block servicecontrol %}
    <section>
        <div class="container">
            <div class="row" id="page-header">
                <div class="col-sm-8 col-sm-offset-2">
                    <h1 class="page-header">{{ 'serviceagent.index.title'|trans }}</h1>
                    {% for serviceagent in serviceagents %}
                        <a href="{{ path('servicecontrol_servicelog_index', {'hash' : community.hash, 'service' : service.id, 'serviceagent' : serviceagent.id }) }}" class="btn btn-primary btn-lg btn-block">{{ serviceagent.name }}&nbsp;{{ icon('arrow-right fa-fw') }}</a>
                    {% endfor %}
                </div>
            </div>
        </div>
    </section>
{% endblock %}
